import { Box, Container, Grid, Stack, Typography } from '@mui/material'
const steps = [['01', 'Create your room', 'Give your session a name and get a shareable code.'], ['02', 'Build the queue', 'Search for a song, paste a link, and add it to the night.'], ['03', 'Take the stage', 'Follow the queue and let the good times roll.']]
function HowItWorks() { return <Box id="how-it-works" component="section" sx={{ py: { xs: 9, md: 14 } }}><Container maxWidth="lg"><Typography variant="overline" color="primary.main" letterSpacing=".15em">HOW IT WORKS</Typography><Grid container spacing={4} sx={{ mt: 1 }}>{steps.map(([number, title, text]) => <Grid key={number} size={{ xs: 12, md: 4 }}><Stack spacing={1.5}><Typography color="secondary.main" fontFamily="monospace" fontWeight={700}>{number}</Typography><Typography variant="h5" fontWeight={700}>{title}</Typography><Typography color="text.secondary" lineHeight={1.7}>{text}</Typography></Stack></Grid>)}</Grid></Container></Box> }
export default HowItWorks
