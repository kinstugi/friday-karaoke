import { useState } from 'react'
import { Avatar, Box, Button, Chip, Divider, Drawer, IconButton, Paper, Stack, Tooltip, Typography } from '@mui/material'
import CloseRoundedIcon from '@mui/icons-material/CloseRounded'
import ContentCopyRoundedIcon from '@mui/icons-material/ContentCopyRounded'
import ExpandRoundedIcon from '@mui/icons-material/ExpandRounded'
import GraphicEqRoundedIcon from '@mui/icons-material/GraphicEqRounded'
import GroupRoundedIcon from '@mui/icons-material/GroupRounded'
import LogoutRoundedIcon from '@mui/icons-material/LogoutRounded'
import QueueMusicRoundedIcon from '@mui/icons-material/QueueMusicRounded'
import ShareRoundedIcon from '@mui/icons-material/ShareRounded'
import StopCircleRoundedIcon from '@mui/icons-material/StopCircleRounded'
import { QRCodeSVG } from 'qrcode.react'

type Panel = 'queue' | 'guests' | 'share' | null

function RoomPage({ title, code, onExit }: { title: string; code: string; onExit: () => void }) {
  const [panel, setPanel] = useState<Panel>(null)
  const [expanded, setExpanded] = useState(false)
  const [endOpen, setEndOpen] = useState(false)
  const inviteUrl = `https://singalong.app/join/${code}`
  const openPanel = (nextPanel: Panel) => setPanel(panel === nextPanel ? null : nextPanel)

  return <Box sx={{ minHeight: '100vh', bgcolor: '#0d0a12', color: 'white', display: 'flex', flexDirection: 'column' }}><Box component="header" sx={{ height: 72, px: { xs: 2, md: 4 }, borderBottom: '1px solid rgba(255,255,255,.1)', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}><Stack direction="row" spacing={1.5} alignItems="center"><GraphicEqRoundedIcon sx={{ color: 'primary.main' }} /><Box><Typography fontWeight={700}>{title}</Typography><Stack direction="row" spacing={1} alignItems="center"><Box sx={{ width: 6, height: 6, borderRadius: '50%', bgcolor: '#6fe3a0' }} /><Typography variant="caption" color="text.secondary">Live room · {code}</Typography></Stack></Box></Stack><Stack direction="row" spacing={.5} alignItems="center"><Tooltip title="Guests"><IconButton onClick={() => openPanel('guests')} color="inherit"><GroupRoundedIcon /></IconButton></Tooltip><Tooltip title="Queue"><IconButton onClick={() => openPanel('queue')} color="inherit"><QueueMusicRoundedIcon /></IconButton></Tooltip><Tooltip title="Share room"><IconButton onClick={() => openPanel('share')} color="inherit"><ShareRoundedIcon /></IconButton></Tooltip><Button onClick={() => setEndOpen(true)} color="secondary" variant="outlined" size="small" startIcon={<StopCircleRoundedIcon />} sx={{ ml: 1, display: { xs: 'none', sm: 'inline-flex' } }}>End session</Button><IconButton onClick={() => setEndOpen(true)} color="secondary" sx={{ display: { xs: 'inline-flex', sm: 'none' } }}><StopCircleRoundedIcon /></IconButton></Stack></Box><Box component="main" sx={{ flex: 1, display: 'flex', justifyContent: 'center', alignItems: 'center', textAlign: 'center', p: 3, background: 'radial-gradient(circle at 50% 40%, #281933 0, transparent 42%)' }}><Stack alignItems="center" spacing={3} sx={{ maxWidth: 700 }}><Chip label="UP NEXT" sx={{ color: 'secondary.main', bgcolor: 'rgba(236,113,151,.12)', letterSpacing: '.12em' }} /><Typography variant="h1" sx={{ fontSize: { xs: '2.7rem', md: '5rem' }, letterSpacing: '-.06em' }}>The stage is<br /><Box component="span" sx={{ color: 'primary.main' }}>yours.</Box></Typography><Typography variant="h6" color="text.secondary" fontWeight={400}>Lyrics will appear here when the first song starts.</Typography><Paper elevation={0} sx={{ px: 3, py: 1.5, bgcolor: 'rgba(255,255,255,.06)', color: 'text.secondary', border: '1px solid rgba(255,255,255,.1)' }}><Typography variant="body2">Add a song to get the room moving</Typography></Paper></Stack></Box><Box component="footer" sx={{ px: { xs: 2, md: 4 }, py: 2, borderTop: '1px solid rgba(255,255,255,.1)', display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}><Typography variant="caption" color="text.secondary">Host controls · Lyrics mode</Typography><Stack direction="row" spacing={1}><Avatar sx={{ width: 28, height: 28, bgcolor: 'secondary.main', fontSize: 12 }}>H</Avatar><Typography variant="caption" color="text.secondary" sx={{ alignSelf: 'center' }}>You’re hosting</Typography></Stack></Box><Drawer anchor="right" open={panel !== null} onClose={() => setPanel(null)} PaperProps={{ sx: { width: expanded ? '100vw' : { xs: '100vw', sm: 400 }, bgcolor: '#20152a', color: 'white', p: 3 } }}><Stack direction="row" alignItems="center" justifyContent="space-between"><Typography variant="h5" fontWeight={700}>{panel === 'queue' ? 'Song queue' : panel === 'guests' ? 'In the room' : 'Share your room'}</Typography><Stack direction="row"><IconButton onClick={() => setExpanded(!expanded)} color="inherit"><ExpandRoundedIcon /></IconButton><IconButton onClick={() => setPanel(null)} color="inherit"><CloseRoundedIcon /></IconButton></Stack></Stack><Divider sx={{ my: 3, borderColor: 'rgba(255,255,255,.1)' }} />{panel === 'queue' ? <QueuePanel /> : panel === 'guests' ? <GuestsPanel /> : <SharePanel code={code} inviteUrl={inviteUrl} />}</Drawer>{endOpen && <Drawer anchor="bottom" open onClose={() => setEndOpen(false)} PaperProps={{ sx: { bgcolor: '#24152f', color: 'white', p: 3, borderRadius: '20px 20px 0 0' } }}><Stack spacing={2} alignItems="center"><StopCircleRoundedIcon color="secondary" fontSize="large" /><Typography variant="h5" fontWeight={700}>End this session?</Typography><Typography color="text.secondary">Your room will close for everyone currently in it.</Typography><Stack direction="row" spacing={1}><Button onClick={() => setEndOpen(false)} color="inherit">Keep singing</Button><Button onClick={onExit} color="secondary" variant="contained">End session</Button></Stack></Stack></Drawer>}</Box>
}

function QueuePanel() { return <Stack spacing={2}><Typography color="text.secondary">No songs have been added yet.</Typography><Button variant="contained" startIcon={<QueueMusicRoundedIcon />}>Add the first song</Button><Box sx={{ mt: 2, p: 2, bgcolor: 'rgba(255,255,255,.05)', borderRadius: 2 }}><Typography variant="body2" fontWeight={700}>Tip</Typography><Typography variant="caption" color="text.secondary">Share the room code so guests can build the queue from their phones.</Typography></Box></Stack> }
function GuestsPanel() { return <Stack spacing={2}><Stack direction="row" spacing={2} alignItems="center"><Avatar sx={{ bgcolor: 'secondary.main' }}>H</Avatar><Box><Typography fontWeight={700}>Host</Typography><Typography variant="caption" color="text.secondary">Running the room</Typography></Box><Chip label="You" size="small" sx={{ ml: 'auto' }} /></Stack><Typography color="text.secondary" sx={{ mt: 2 }}>Waiting for guests to join…</Typography></Stack> }
function SharePanel({ code, inviteUrl }: { code: string; inviteUrl: string }) { return <Stack spacing={3} alignItems="center" textAlign="center"><Typography color="text.secondary">Scan the code or send this link to your guests.</Typography><Box sx={{ p: 2, bgcolor: 'white', borderRadius: 2 }}><QRCodeSVG value={inviteUrl} size={190} /></Box><Typography variant="h5" fontWeight={700} letterSpacing=".12em">{code}</Typography><Button fullWidth variant="outlined" color="inherit" startIcon={<ContentCopyRoundedIcon />}>Copy invite link</Button></Stack> }

export default RoomPage
